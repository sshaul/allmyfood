import { StyleSheet } from 'react-native';

export default text = StyleSheet.create({
    title: {
      flex: 1,
      backgroundColor: '#ff2',
      alignItems: 'center',
      justifyContent: 'flex-start',
      fontSize: 45
    },
    groceryItem: {
      flex: 1,
      fontSize: 20,
    }
  });